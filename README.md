# HeightmapToTable
A Streamlit web app that converts heightmap images into structured XYZ coordinate tables. Upload a bw heightmap picture, define your grid size and value range -> get a downloadable table of elevation data instantly
